{
  "edit-id": "interface_state",
  "operation": "{{ 'merge' if not interface_enabled else 'remove' }}",
  "target": "/interface/{{ interface_type }}={{ interface_name }}/shutdown"{{ "," if not interface_enabled }}
{% if not interface_enabled %}
  "value": {
    "Cisco-IOS-XE-native:shutdown": ""
  }
{% endif %}
}